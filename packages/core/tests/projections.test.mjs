import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {CORE_OPERATION_CATALOG} from '../src/index.mjs';
test('generated agent, CLI, API, and documentation projections share one Omniform contract',()=>{const canonical=CORE_OPERATION_CATALOG.operations.find(item=>item.id==='generate_plan'),tools=JSON.parse(fs.readFileSync('generated/agent-tools.json','utf8')),cli=JSON.parse(fs.readFileSync('generated/cli-operations.json','utf8')),openapi=JSON.parse(fs.readFileSync('generated/openapi.operations.json','utf8')),doc=fs.readFileSync('docs/reference/generated-operations/generate_plan.md','utf8');assert.equal(tools.tools.find(item=>item.name==='generate_plan').description,canonical.description);assert.deepEqual(cli.commands.find(item=>item.operation==='generate_plan').inputSchema,canonical.input);assert.equal(openapi.paths['/operations/generate_plan'].post.summary,canonical.description);assert.match(doc,new RegExp(canonical.description.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')))});
