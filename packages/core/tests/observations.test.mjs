import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
import {evaluateAssertion,MockSemanticEvaluator} from '../src/observations.mjs';
test('assertion observation deterministically references active resource evidence',()=>{const result=evaluateAssertion({id:'support_agent_active',type:'assertion',capability:'customer_support',condition:{resource:'support_agent',state:'active'}},{version:2,resources:{support_agent:{status:'active'}}});assert.deepEqual(result,{id:'execution_support_agent_active_2',observationId:'support_agent_active',capabilityId:'customer_support',type:'assertion',status:'satisfied',evidenceReferences:['resource:support_agent'],evaluator:{name:'omniseed.assertion',version:'0.1.0'}})});
test('mock semantic evaluator returns structured findings',async()=>{const evaluator=new MockSemanticEvaluator([{summary:'Risk changed',impact:'high',urgency:'soon'}]);const [finding]=await evaluator.evaluate({id:'risk',capability:'support'},[{id:'evidence:1'}]);assert.equal(finding.observationId,'risk');assert.deepEqual(finding.evidenceReferences,['evidence:1']);assert.equal(typeof finding.confidence,'number');assert.ok(finding.evaluator.version)});
