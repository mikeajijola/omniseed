import fs from 'node:fs';import path from 'node:path';import {FileStateStore,FileDefinitionStore,FileRuntimeMetadataStore} from '../../core/src/stores.mjs';import {OmniSeedHost} from '../../core/src/host.mjs';import {createRuntimeServer} from './http.mjs';
const directory=process.env.OMNISEED_EXAMPLE||'examples/minimal',read=name=>JSON.parse(fs.readFileSync(`${directory}/${name}`)),port=Number(process.env.PORT||8787),root=path.resolve(process.env.OMNISEED_DATA_DIR||'.omniseed/companies'),companyId=process.env.OMNISEED_COMPANY_ID||read('company.json').company.id;
const host=await OmniSeedHost.open({companyId,definitionStore:new FileDefinitionStore(root),stateStore:new FileStateStore(root),metadataStore:new FileRuntimeMetadataStore(root),bootstrapDefinition:companyId===read('company.json').company.id?read('company.json'):undefined,bootstrapState:companyId===read('company.json').company.id?read('deployment-state.json'):undefined});
createRuntimeServer(host,{corsOrigin:process.env.CORS_ORIGIN||'http://localhost:3000'}).listen(port,()=>console.log(`OmniSeed runtime for ${companyId} listening on http://localhost:${port}; data: ${root}`));
