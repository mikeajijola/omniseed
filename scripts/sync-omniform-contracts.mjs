import fs from 'node:fs';import path from 'node:path';
const source=process.env.OMNIFORM_ROOT?path.join(process.env.OMNIFORM_ROOT,'contracts/core.operations.json'):path.resolve('../omniform/contracts/core.operations.json');const target=path.resolve('generated/omniform-core.operations.json');if(!fs.existsSync(source)){if(process.argv.includes('--check')){console.log('Omniform sibling unavailable; validating committed materialization.');JSON.parse(fs.readFileSync(target,'utf8'));process.exit(0)}throw new Error(`Omniform contract source not found: ${source}`)}const canonical=fs.readFileSync(source,'utf8');if(process.argv.includes('--check')){if(fs.readFileSync(target,'utf8')!==canonical)throw new Error('Generated OmniSeed contract has drifted from Omniform');console.log('Generated OmniSeed contract matches Omniform.')}else{fs.mkdirSync(path.dirname(target),{recursive:true});fs.writeFileSync(target,canonical);console.log('Materialized Omniform operation catalogue into OmniSeed.')}
